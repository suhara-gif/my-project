# CLAUDE.md — my-project 運用マニュアル

このリポジトリで作業する**モデル向けの操作マニュアル**です。より能力の低いモデルでも、
このリポジトリの不変条件を壊さずに変更できることを目的に書いています。
「なんとなく良さそう」ではなく、**チェック可能な基準**として書いています。

## このリポジトリは何か

Claude Code / Fable のローカル運用を支える自己完結な「キット」集(`claude-backup/`・
`second-brain/`・`fable5-agent-system/`・`company/`)。**全体像はルート `README.md` の
キット表を先に読む。** 各キットの詳細はそれぞれの `README.md`。

中核は `claude-backup` の責務分離:
- **①固め(機械的)** `run-backup.sh` が tar 圧縮・秘密除外・暗号化を確実に行う。
- **②転送(決定的)** 同期フォルダ `cp` か `rclone`。**バイナリは MCP/LLM に絶対に渡さない**
  (`second-brain` の Notion ミラーも同じ作法)。
- **③台帳(知的)** `claude -p` + Notion MCP が差分を要約してテキスト1行を記録するだけ。

## 守っている規約(あなたも守ること)

- **対象プラットフォームは macOS(BSD userland / bash 3.2)と Linux(GNU)の両方。**
  これが最も破られやすい前提。下記「壊しやすい間違い」を必ず読むこと。
- すべてのスクリプトは `set -euo pipefail`。新規スクリプトも同じ。
- コメント・ドキュメント・ログ・Notion 台帳の文言は**日本語**。既存の語調に合わせる。
- 設定はすべて環境変数で上書き可能にし、`${VAR:-default}` の形で既定値を持たせる。
  新しい設定を足すときは README の環境変数表と `install.sh` の config 雛形も更新する。
- 破壊的操作の前には必ず退避する(例: `restore.sh` は既存 `~/.claude` を
  `.before-restore.<stamp>` に mv してから展開する)。この作法を外さない。
- CI は `claude-backup/*.sh` と `second-brain/*.sh` に対する **shellcheck**(両方を実行する。
  `.github/workflows/shellcheck.yml`)。警告ゼロを維持する。意図的に無効化するときは
  `# shellcheck disable=SCxxxx` に**理由コメントを必ず併記**する(既存例に倣う)。

## 壊しやすい間違い（名前付き）と、それを防ぐ規則

弱いモデルがこのコードベースで犯しがちな具体的ミス。

**シェル固有の9項目**(BSD/GNU 移植性、データ喪失、秘密除外、MCP へのバイナリ、再帰ガード、
プロンプト・インジェクション、フック環境の変数、install 冪等性、スケジュール実行の PATH)と
**シェルの品質バー**は `.claude/rules/shell-kits.md` にある。`claude-backup/` と `second-brain/`
の `.sh` を読むと自動で読み込まれる。シェルを触るなら、まずそのファイルを開くこと。

以下はキットを跨いで効くため、ここに置く。

**LLM プロンプトに「あなたは〇〇の専門家です」を付ける。**
`claude -p` のテンプレや fable5 の `system_prompt_template` を新しく書くとき、
専門家ペルソナを付ければ賢くなると考えてしまう。精度が上がる根拠は薄く、代わりに
**本来書くべき手順・出力形式・確認事項が痩せる**。
- 規則: 事実調査・コード生成・デバッグ・分析のプロンプトに専門家ペルソナを付けない。
  **やること(手順・入力・出力形式・禁止事項)を書く。** 既存の「あなたは〜係です」+
  手順 + 出力形式が基準(`run-backup.sh` の「台帳の記録係」、`second-brain/*.sh` の
  「コンパイル係 / lint 係 / 採掘係 / 転記係」)。例外は2つだけ:
  ①ドキュメントの見直しに視点を与える場合(決裁者視点・初めて読む人視点。
  `.claude/commands/up-verify.md` の「独立した検品担当」)、
  ②子セッション/サブエージェントの役割定義(1エージェント=1役割)。

## 成果物ごとの品質バー（形容詞ではなくチェック項目）

**シェルスクリプトの変更**(`claude-backup/*.sh` / `second-brain/*.sh`):
`.claude/rules/shell-kits.md` のチェック項目を使う(該当の `.sh` を読むと自動で載る)。

**ドキュメント変更**(`README.md` 各所):
- [ ] 環境変数を足したら README の環境変数表・`install.sh` の config 雛形の**両方**を更新。
- [ ] 手順は macOS/Linux 双方で成立するように書く(片方だけの前提を書かない)。

**新しい設定項目**:
- [ ] `${VAR:-default}` で既定値を持つ。
- [ ] `config` 経由(フック環境)でも効く。
- [ ] README の表に1行追加。

**調査・分析の出力**(`docs/learnings/`、健診ログ、台帳記録、共有用メモ):
- [ ] 事実確認が要る内容は「初稿 → 検証質問 → 初稿を見ずに回答 → 最終版」を通した。
- [ ] 未確認の数値・固有名詞・日付に ［要確認：…］ が付いている。不明は「不明」と書いた。
- [ ] 確度をパーセンテージで書いていない。

## 計画・設計

### オーケストレーションの判断
並行サブエージェント(`Workflow` / `Agent`)を使うかは**ユーザーに判断させず、こちらが判定して提案する**。
既定は単一セッション。**各片が他の片の出力を必要としない**ことが並行化の必須条件。
コード変更のファンアウトと検品目的は原則向かない(検品はまず `/up-verify`)。
非対話セッション(フック / launchd / cron)では提案も起動もしない。詳細: docs/orchestration-policy.md

### 計画時の盲点チェック
計画・設計タスクの着手時、ユーザーに提示する前に必ず以下を実行する:
「この計画の暗黙の前提と考慮漏れリスクを列挙する。ただし一般論ではなく、渡された資料・コンテキストに基づく指摘に限定する。検証材料がない領域（人間関係・口頭の経緯・相手企業の内部事情）は『AIでは検証不可』と明示する」

## 調査・分析の作法（推測で埋めないための手順）

コードの正しさは shellcheck と品質バーが守る。**調査・分析の出力を守る仕組みはここだけ**で、
実際に事故っている(2026-08-09「領収書が来ない＝配信停止」の誤断定:
docs/learnings/20260809-absence-of-evidence-needs-a-control.md)。

### 読み方: 構成を先に、必要な箇所だけ

大きな資料やコードベースを最初から全部読み込まない。まず**構成と各ファイルの役割**を掴み、
担当箇所を特定してから、その範囲だけを開く。
- このリポジトリなら: ルート `README.md` のキット表 → 該当キットの `README.md` →
  そのキットのファイル、の順。他キットのファイルは開く理由ができるまで開かない。
- 例外は機械が全走査するもの(`shellcheck claude-backup/*.sh` など)。

### 事実確認が要る作業は、初稿と裏取りを分ける

調査・仕様把握・外部情報の要約など**事実確認が要る作業**では、初稿をそのまま出さない。
次の4つを**別のステップとして**実行する:
1. 初稿を書く。
2. 初稿のうち事実誤認しうる箇所を**検証質問に変換する**(「この数値の出典はどのファイルの
   どの行か」「この挙動は実際に実行して確認したか」の形にする)。
3. **初稿を見ずに**検証質問へ答える(資料・実行結果・`--help` などの一次情報で)。
4. 3 の答えを反映した最終版を出す。

作る係と検品係を分ける `/up-verify`(`.claude/commands/up-verify.md`)は 3 を外部化したもの。
長い成果物ではこちらを使ってよい。

### 未確認の値には ［要確認］ を付ける

数値・固有名詞・日付のうち**自分で出典を確認できていないもの**には ［要確認：何を確認するか］
を付ける(`company/knowledge/business.md` の既存表記に合わせる)。
- 分からないものは「不明」と書く。**推測で埋めない。**
- 確度をパーセンテージで書かない(「70% 確実」のような擬似的な精度を出さない)。
- このルールは**コード内コメントには適用しない**(コードは動作が真偽を決めるため)。

## 迷ったときのエスカレーション規則

以下は**自分で判断せず、`AskUserQuestion` で人間に確認する**:
- 秘密除外パターンを外す/緩める、あるいは認証情報をアーカイブに含める変更。
- `claude -p` の `--allowedTools` を広げる、または子に新しい権限を与える変更。
- 既存の設定・データ・スケジュールを削除/上書きする挙動(退避なしの破壊的操作)。
- 「対象プラットフォーム(macOS BSD / Linux)の一方を切り捨てる」判断。
- Notion 台帳DBのスキーマを非互換に変える(既存列の削除・型変更)。

以下は**確認不要で進めてよい**(ただし上の品質バーを満たすこと):
- shellcheck 指摘の修正、コメント・ログ文言の改善、README の追記、既定値の追加、
  新しい**任意**環境変数の追加(既定オフで後方互換)。

判断がつかないときは、**壊れない側(データを残す / 除外を強める / 権限を狭める)**に倒す。

## 学びの記録法（learning law）

非自明な問題を解いたら、次に進む前に `extract-approach` スキルを実行し、
`docs/learnings/` に1件の学びノートを残すこと。**学びノートの無い解決は未完了とみなす。**
スキルの定義は `.claude/skills/extract-approach/SKILL.md`。

シェル変更をコミットする前には `shell-safety-check` スキルで上の品質バーを機械的に確認する。
定義は `.claude/skills/shell-safety-check/SKILL.md`。
