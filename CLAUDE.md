# CLAUDE.md — my-project 運用マニュアル

これはユーザー(須原弘之さん)個人の作業リポジトリです。**特定の1プロジェクトに限定せず、
複数の独立したキット・プロジェクト・日々の相談ごとを扱う場所**として使います。
コードの実装作業から、秘書(アプ子)への雑多な相談まで、何でもここで扱ってよいです。

## リポジトリの構成

| ディレクトリ | 内容 |
|---|---|
| [`claude-backup/`](claude-backup/) | `~/.claude/` をクラウドへ自動退避するシェルキット。詳細ルールは [`claude-backup/CLAUDE.md`](claude-backup/CLAUDE.md) |
| [`second-brain/`](second-brain/) | Obsidian vault をセカンドブレイン化するキット。詳細ルールは [`second-brain/vault-template/CLAUDE.md`](second-brain/vault-template/CLAUDE.md) |
| [`company/`](company/) | AI秘書「アプ子」(`/up`, `/my-secretary`)まわりの設定・ナレッジ・日報 |
| [`fable5-agent-system/`](fable5-agent-system/) | Claude API 上の自己改善型エージェントシステムの実装(Python) |
| [`docs/learnings/`](docs/learnings/) | 非自明な問題を解いたときの学びノート置き場(リポジトリ横断) |
| [`.claude/skills/`](.claude/skills/) | このリポジトリで使うカスタムスキル |

各プロジェクト固有の規約(対応プラットフォーム、品質バー、エスカレーション基準など)は、
そのディレクトリ配下にある `CLAUDE.md` または `README.md` を参照すること。ここに書くのは
**リポジトリ全体に共通する**working habit のみ。

## リポジトリ横断で守ること

- ドキュメント・コメント・ログの文言は基本**日本語**。既存の語調に合わせる。
- 破壊的操作(削除・上書き)の前には必ず退避・確認する。各プロジェクトの詳細ルールが
  優先するが、迷ったら**壊れない側**に倒す。
- どのプロジェクトで作業する場合も、そのディレクトリの `CLAUDE.md`/`README.md` を
  先に確認してから着手する。

## 計画・設計

### 計画時の盲点チェック
計画・設計タスクの着手時、ユーザーに提示する前に必ず以下を実行する:
「この計画の暗黙の前提と考慮漏れリスクを列挙する。ただし一般論ではなく、渡された資料・コンテキストに基づく指摘に限定する。検証材料がない領域(人間関係・口頭の経緯・相手企業の内部事情)は『AIでは検証不可』と明示する」

### 観察を昇格させるときの前提チェック
上の盲点チェックは**着手時**にしか発火しない。報告・提案・分析を**書いている途中**、
観察したことを**推奨アクション・断定・数値**として書く直前にも必ず以下を実行する:
「この観察が成り立つために必要な前提を箇条書きにし、各前提へ『確認済み / 未確認』を付す。
未確認が1つでも残るなら、推奨アクションにせず `[要確認]` に留める」

未確認の観察を `[要確認]` に置くのはコスト0。**推奨アクションに昇格させた瞬間にコストが発生する。**
移行・置換の提案では、移行先のコスト(単価・制約)を必ず1つ確認してから書く。
「確認すると自分の発見が消えるかもしれない」と感じた確認は、最優先で実行する。
経緯: [docs/learnings/20260817-promote-only-after-listing-assumptions.md](docs/learnings/20260817-promote-only-after-listing-assumptions.md)

## 学びの記録法(learning law)

非自明な問題を解いたら、次に進む前に `extract-approach` スキルを実行し、
`docs/learnings/` に1件の学びノートを残すこと。**学びノートの無い解決は未完了とみなす。**
スキルの定義は `.claude/skills/extract-approach/SKILL.md`。

プロジェクト固有の検証スキル(例: claude-backup の `shell-safety-check`)は、
対象ディレクトリの `CLAUDE.md` に従って実行する。
