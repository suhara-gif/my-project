---
description: 広告データの手動点検（オンデマンド診断）。監視ではない
---

**あなた自身は点検しないでください。** 必ず Agent tool で `up-check-runner` サブエージェントを
呼び出し、点検を委譲すること。理由: シートは巨大で、メインの会話コンテキストで直接処理すると
トークンを大きく消費する。独立コンテキストに追い出すことで、この会話を圧迫しない。

追加の指示があれば、そのままタスクメッセージに含めて委譲すること。

$ARGUMENTS
